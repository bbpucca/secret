<h1>Register if you want to know the answer to the Universe !</h1>

<center><% if logged_in? %>
  <h2><a href="static_pages/secret">Get the answer</a></h2>
<% else %>
  <h2><a href="login" data-confirm="You have to log in first">Get the answer</a></h2>
<% end %>

    <% if logged_in? %>

    <% else %>
      <%= link_to "Create an account", signup_path %>
    <% end %>

  <% if @users.length >= 1 %>
    <p>There are<%= @users.length %> user</p>
  <% else %>
    <p>Discover the answer</p>
  <% end %></center>
<center><img src="https://media.giphy.com/media/ygRvBA8SWudws/giphy.gif" alt="Confused Pokemon GIF" style="width: 480px; height: 359.76px; opacity: 1; left: 0px; top: 0px;"></center>

